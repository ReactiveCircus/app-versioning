package io.github.reactivecircus.appversioning

class AppVersioningPluginFunctionalTest {

    // TODO `plugin cannot be applied to project with AGP version lower than the minimum AGP version required`
    // TODO `GenerateAppVersionInfo task fails when root project is not a git repository`
    // TODO `GenerateAppVersionInfo task fails when maxDigits is lower than minimum maxDigits required`
    // TODO `GenerateAppVersionInfo task fails when maxDigits is higher than maximum maxDigits required`
}
