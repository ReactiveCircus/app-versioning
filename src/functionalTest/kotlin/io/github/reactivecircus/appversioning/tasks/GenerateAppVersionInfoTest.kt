package io.github.reactivecircus.appversioning.tasks

class GenerateAppVersionInfoTest {
    // TODO `GenerateAppVersionInfo task fails when root project is not a git repository`
    // TODO `GenerateAppVersionInfo task fails when maxDigits is lower than minimum maxDigits required`
    // TODO `GenerateAppVersionInfo task fails when maxDigits is higher than maximum maxDigits required`
    // TODO test with `releaseBuildOnly`, `requireValidGitTag`, `maxDigits`, `overrideVersionCode`, `overrideVersionName`
    // TODO `GenerateAppVersionInfo supports incremental build`
    // TODO `GenerateAppVersionInfo is cacheable`
}
